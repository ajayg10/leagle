# .env.example — SAFE TO COMMIT
# Copy this file and rename to .env, then fill in real values

# Neon DB (Serverless PostgreSQL)
# Get your connection string from https://console.neon.tech
# IMPORTANT: Change postgresql:// to postgresql+asyncpg:// for async operations
DATABASE_URL=postgresql+asyncpg://your_neon_user:your_neon_password@your-region.neon.tech/compliance_db?sslmode=require

# Qdrant (Vector Database)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=regulations_v1

# Redis (Cache & Message Queue)
REDIS_URL=redis://localhost:6379/0

# LLM — Google Gemini (free tier)
# Get free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# App Configuration
SECRET_KEY=your_super_secret_key_here_minimum_32_characters_required_for_production
ENVIRONMENT=development

# Optional: LLM fallback to OpenAI (leave empty to use Gemini)
# OPENAI_API_KEY=

# ── Clerk Authentication (REQUIRED in production) ────────────────────────────
# Find these in your Clerk Dashboard → API Keys
#
# JWKS URL: https://<your-clerk-domain>/.well-known/jwks.json
CLERK_JWKS_URL=https://your-clerk-domain/.well-known/jwks.json

# Issuer: your Clerk Frontend API URL (e.g. https://clerk.your-domain.com)
CLERK_ISSUER=https://your-clerk-domain

# Audience (optional): only set if you configured `azp` / allowed origins in Clerk
# CLERK_AUDIENCE=

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Use your actual production domain.
# In development, defaults to http://localhost:3000 if left empty.
ALLOWED_ORIGINS=http://localhost:3000,https://your-production-domain.com
